var Validator = require("validator");

describe("a", function () {
    it("b", function () {
        expect(1).toEqual(1);
    });
    it("c", function () {
        expect(1).toEqual(1);
    });
});